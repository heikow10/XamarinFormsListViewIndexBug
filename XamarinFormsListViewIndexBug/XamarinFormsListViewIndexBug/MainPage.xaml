<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XamarinFormsListViewIndexBug"
             x:Class="XamarinFormsListViewIndexBug.MainPage"
             x:DataType="local:MainPage">
    <StackLayout Orientation="Vertical">
        <Button Text="ScrollTo 3" Clicked="ScrollToButton_Clicked" />
        <ListView x:Name="MyListView" ItemsSource="{Binding Path=Items}" Header="" HasUnevenRows="True" ItemSelected="ListView_ItemSelectedAsync" ItemTapped="ListView_ItemTappedAsync">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid>
                        <Label Text="Header" BackgroundColor="LightGray" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <ViewCell>
                        <ViewCell.ContextActions>
                            <MenuItem Text="Context" Clicked="MenuItem_ClickedAsync" />
                        </ViewCell.ContextActions>
                        <Label Text="{Binding Path=.}" />
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>
