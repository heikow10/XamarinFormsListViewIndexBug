<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XamarinFormsListViewIndexBug"
             x:Class="XamarinFormsListViewIndexBug.MainPage"
             x:DataType="local:MainPage">
    <ListView ItemsSource="{Binding Path=Items}" ItemSelected="ListView_ItemSelectedAsync" ItemTapped="ListView_ItemTappedAsync">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="x:String">
                <ViewCell>
                    <ViewCell.ContextActions>
                        <MenuItem Text="Context" Clicked="MenuItem_ClickedAsync" />
                    </ViewCell.ContextActions>
                    <Label Text="{Binding Path=.}" />
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>
